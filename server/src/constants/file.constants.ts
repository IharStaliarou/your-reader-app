export const MAX_FILE_SIZE_BYTES = 5 * 1024 * 1024;
export const UPLOAD_DESTINATION = './uploads';
export const ALLOWED_MIME_TYPES = ['application/pdf', 'text/plain'];

export const ALLOWED_MIME_TYPES_MSG =
  'Invalid file type. Only PDF and TXT are allowed.';

export const MULTER_FIELD_NAME = 'file';
